---
name: "󰋜 Home"
windows:
  - config:
      root: ~/.config/yadm
      layout: main-horizontal
      panes:
        - v
        - htop
        - while true; do ctop; sleep 5; done
        - cd && while true; do clear && yadm st; sleep 60; done
  - journal:
      root: ~/.Notes
      layout: main-horizontal
      panes: [journal]
