---
windows:
  - blog:
      root: ~/Code/mikedfunk.github.io
      layout: main-horizontal
      panes:
        - editor: [v]
        - git: [ts]
        - jekyll: [bundle install && bundle exec jekyll serve --watch]
